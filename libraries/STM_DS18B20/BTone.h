// ----------------------------------------------------------------------------

// ----------------------------------------------------------------------------

#ifndef __BTone_h__
#define __BTone_h__

// ----------------------------------------------------------------------------

#include <Arduino.h>

// ----------------------------------------------------------------------------



// ----------------------------------------------------------------------------

class BTone{
	public:
		BTone(uint8_t pin){	Pin = pin; pinMode(Pin, OUTPUT); digitalWrite(Pin, LOW);};
		void service(void);
		void tone(int duration);	// ms
		void noTone(void);
	private:
		uint8_t Pin;
		boolean State = 0;
		uint32_t StopTime;
};

// ----------------------------------------------------------------------------

#endif // __RotatyEncoderMenu_h__
